<!DOCTYPE html>
<html>
<head>
  <title>Admin</title>

</head>
<body>
<%= render 'layouts/nav' %>
<h1>Admin</h1>

<% flash.each do |key, value| %>
    <div class="<%= flash_class(key) %>">
          <%= value %>
    </div>
<% end %>

<%= yield %>

</body>
</html>