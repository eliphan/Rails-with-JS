<!DOCTYPE html>
<html>
<head>
  <title>Admin</title>

</head>
<body>

<h1>Admin</h1>
<div class="container">
  <% flash.each do |key, value| %>
    <div class="<%= flash_class(key) %>">
      <%= value %>
    </div>
  <% end %>

  <%= yield %>
</div>

<%= yield %>

</body>
</html>